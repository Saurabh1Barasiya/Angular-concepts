import { CapitalizeImpurePipe } from './capitalize-impure.pipe';

describe('CapitalizeImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
