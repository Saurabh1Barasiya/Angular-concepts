import { CapitalizePurePipe } from './capitalize-pure.pipe';

describe('CapitalizePurePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizePurePipe();
    expect(pipe).toBeTruthy();
  });
});
