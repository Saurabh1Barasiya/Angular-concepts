import { FilterImpurePipe } from './filter-impure.pipe';

describe('FilterImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
