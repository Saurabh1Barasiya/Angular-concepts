import { FilterPurePipe } from './filter-pure.pipe';

describe('FilterPurePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPurePipe();
    expect(pipe).toBeTruthy();
  });
});
